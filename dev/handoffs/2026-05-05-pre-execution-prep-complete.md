# Handoff — 2026-05-05 → Next Session (v0.0.1 Task 1 begins)

**From agent:** alder
**Session arc:** Pre-execution prep wave. No application code; governance, release infrastructure, AI-orchestration scaffolding, and decision records put in place before Task 1.
**Status:** Prep complete. Pushed to origin. Ready to begin Task 1.

---

## Next session's starting prompt

Verbatim statement to paste into the fresh Claude Code session (see "Tooling refresh BEFORE this prompt fires" below — finish that first):

> I'm resuming the tuxlink project. The pre-execution prep wave finished in the previous session (agent `alder`); we're at the v0.0.1 Task 1 boundary. Read these in order before doing anything else:
>
> 1. `dev/handoffs/2026-05-05-pre-execution-prep-complete.md` — this handoff. Captures the full project state, the override mechanism for bd's CLAUDE.md additions, and the first-action workflow.
> 2. `CLAUDE.md` — pay particular attention to the `## Tool referee` section, which overrides three of bd's BEADS INTEGRATION directives (TodoWrite ban, MEMORY.md ban, mandatory-push). Do NOT follow bd literally on those three rules.
> 3. `docs/adr/` — six ADRs are in place. `0004-per-task-branch-model.md` and `0006-override-bd-claude-md-defaults.md` are the most operationally load-bearing.
> 4. `docs/plans/2026-04-22-tuxlink-v0.0.1-plan.md` — Task 1 begins here. Commit heredocs use `Agent: <SESSION-MONIKER>` placeholders (substitute your moniker before committing). Note the Per-Task Branch Wrap section codifying the workflow.
> 5. `docs/pitfalls/implementation-pitfalls.md` and `docs/pitfalls/testing-pitfalls.md` per CLAUDE.md prerequisites.
>
> Once read:
>
> - Pick a fresh session moniker (NOT `alder`). Pre-flight against BOTH `grep -rci "<name>" /home/administrator/Code/tuxlink/` AND `git log --all --grep="^Agent: <name>" --since="3 days ago"` per `feedback_moniker_collision_pre_flight.md` in auto-memory.
> - Run `bd ready` to confirm `tuxlink-wkz` (Task 1) is the next available work, then `bd update tuxlink-wkz --claim`.
> - Branch off `feat/v0.0.1` with `git checkout -b task-01-tauri-scaffold` (per ADR 0004 per-task-branch model).
> - Invoke `superpowers:subagent-driven-development` — the plan's REQUIRED SUB-SKILL for execution. Alternative is `superpowers:executing-plans` for inline; default to subagent-driven unless directed otherwise.
> - Begin Task 1's first step (Tauri prerequisites verification).
>
> Engineering controls now active:
> - PreToolUse hooks reject destructive git, missing/unsubstituted `Agent:` trailers, and direct commits to `main` / `feat/v0.0.1` (the latter has an `ALLOW_INTEGRATION_COMMIT=1` env-var carve-out for legitimate squash-merges only).
> - SessionStart hook prints a briefing on session boot — if it didn't, something is wrong with the hook installation.
> - Branch protection on main blocks force-push and deletion at the GitHub layer.
>
> bd's local issue database has the full 18-task graph; cross-session state lives there. Use TodoWrite for in-turn working memory only. Auto-memory at `~/.claude/projects/-home-administrator-Code-tuxlink/memory/` is already seeded — read MEMORY.md for the index.
>
> Surface any inconsistency between bd's directives and the `## Tool referee` table before acting on it. If bd's CLAUDE.md section was regenerated by `bd setup claude` since the last session, see BD-1 in `docs/pitfalls/implementation-pitfalls.md`.

---

## Tooling refresh — DO BEFORE the prompt above

Run these in a regular shell (not inside Claude Code), in order, before opening the next Claude Code session:

```bash
# 1. Pull gstack updates (25 commits behind v1.26.3.0).
git -C ~/.claude/skills/gstack pull --ff-only origin main

# 2. Superpowers is already at 5.1.0 — NO update needed.
#    (Verified 2026-05-05 via `claude plugin update superpowers@claude-plugins-official`,
#    which reported "already at the latest version (5.1.0)".)
#    If a future session needs to force a re-check:
#       claude plugin update superpowers@claude-plugins-official
#    NOTE the marketplace suffix is REQUIRED — bare `superpowers` does not resolve.

# 3. /quit any active Claude Code session — a restart is required
#    for the SessionStart + PreToolUse hooks to attach to the watcher.
#    (Hooks were registered mid-session in alder's run, after the
#    watcher had already enumerated its directories; they did not
#    fire in alder's session and will only attach on the next boot.)

# 4. Open a fresh Claude Code session against /home/administrator/Code/tuxlink.

# 5. Verify gstack:    git -C ~/.claude/skills/gstack log --oneline -1
#    should show v1.26.3.0+ (or whatever was latest at refresh time).
```

Once the new session opens, the SessionStart hook should print a briefing automatically (current branch, uncommitted state, recent commits, most-recent handoff filename). If no briefing appears, the hook didn't fire — investigate before proceeding.

---

## State at pause

### What's pushed to origin

```
f81b7ad  chore: pre-execution scaffolding — governance, hooks, ADRs, plan patches  ← main, feat/v0.0.1
9face6d  bd init: initialize beads issue tracking
b8c65a6  docs(plan): drop fake CMS; add Part 97 consent gate + live-CMS policy
0565381  docs(plan): v0.0.1 implementation plan + supporting guardrails
168f4c8  chore: initial project framing
```

Both `main` and `feat/v0.0.1` are at `f81b7ad` on origin. Branch protection on main is active.

### What's NOT pushed

- bd's dolt issue database — solo-use deferral. Issue *definitions* are durable in `.beads/issues.jsonl` (committed); only *operational state* (claimed/closed) is local-only. Set up later via `bd dolt remote add origin <url>` if cross-machine sync is needed.

### What's working tree state

Clean. No uncommitted changes.

---

## What landed in the prep wave

**Governance & contributor docs** — `LICENSE` (MIT, matches Pat upstream), `CODE_OF_CONDUCT.md` (link-style Contributor Covenant 2.1), `CONTRIBUTING.md` (Conventional Commits, scope table, per-task-branch workflow, mandatory `Agent:` trailer), `SECURITY.md` (private vuln disclosure, supported versions), `VERSIONING.md` (project-specific SemVer with pre-1.0 behavior locked, hotfix recipe), `UPGRADING.md` (stub), `CHANGELOG.md` (release-please-managed from v0.0.2 onward).

**Release infrastructure** — `.github/workflows/release-please.yml` runs on every push to `main`. `.github/release-please-config.json` configures `bump-minor-pre-major: true` so pre-1.0 breakings bump minor instead of jumping to 1.0.0. `.github/.release-please-manifest.json` anchors at `0.0.0`. `.github/dependabot.yml` runs weekly cargo / npm / github-actions update checks (already triggered on first push — see "Open Dependabot PRs" below). PR template + 3 issue templates (`bug_report.yml`, `feature_request.yml`, `config.yml`) in `.github/`.

**Architecture Decision Records** — six ADRs in `docs/adr/`:
- 0001 Record architecture decisions (the meta-ADR).
- 0002 Tauri 2 + React + single-crate architecture for v0.0.1.
- 0003 Pat owns the mailbox; no SQLite in v0.0.1.
- 0004 Per-task branch model with squash-merge.
- 0005 Rigorous SemVer via release-please.
- 0006 Override bd's CLAUDE.md defaults via external Tool referee.

**Engineering controls (Claude Code hooks)** — `.claude/settings.json` registers three hooks:
- `block-destructive-git.sh` (PreToolUse Bash) — denies 13 banned patterns from CLAUDE.md (reset --hard, force-push, --no-verify, --amend, filter-branch, clean -f, etc.).
- `check-commit-discipline.sh` (PreToolUse Bash on `git commit`) — refuses unsubstituted `<SESSION-MONIKER>` placeholders, missing `Agent:` trailers, direct commits to main / feat/v0.0.1 (`ALLOW_INTEGRATION_COMMIT=1` env-var prefix carves out the legitimate squash-merge step).
- `session-start-briefing.sh` (SessionStart) — emits branch state + recent handoff + recent commits as `additionalContext`.

All three pipe-tested before commit. **Hooks did not fire in alder's session** because the settings watcher only watches dirs that had a settings file at session start. Hooks fire from the next session onward — that's why this handoff exists.

**Tool referee mechanism (bd override)** — see ADR 0006. Three of bd's BEADS INTEGRATION directives ("do NOT use TodoWrite," "do NOT use MEMORY.md," "PUSH TO REMOTE — MANDATORY") are explicitly overridden via the `## Tool referee` section in CLAUDE.md (placed BEFORE bd's markers, so bd's update logic doesn't touch it). AGENTS.md has a summary pointer. Pitfall BD-1 in `docs/pitfalls/implementation-pitfalls.md` captures the drift signature + fix recipe + review checklist.

**Plan patches** in `docs/plans/2026-04-22-tuxlink-v0.0.1-plan.md`:
- 23 instances of `Agent: lichen` → `Agent: <SESSION-MONIKER>` placeholder.
- New Per-Task Branch Wrap section codifying ADR 0004's workflow at the per-task level.
- Subagent Guardrails branch bullet updated for per-task model.
- Stale `superpowers/5.0.7/...` SKILL.md path replaced with version-agnostic glob.

**Beads** — installed at `~/.local/bin/bd` (v1.0.3, ARM64, checksum-verified). 18 v0.0.1 plan tasks ported with full dependency graph. `bd ready` returns Task 1 (`tuxlink-wkz`); `bd dep tree tuxlink-n65` shows the transitive chain to Task 19. `bd setup claude` integrated SessionStart `bd prime` and PreCompact `bd sync` hooks (alongside our destructive-git/commit-discipline/briefing hooks).

**Branch protection** on main: no force-push, no deletion. Per-task-branch model takes effect from `f81b7ad` forward.

**Memory seed** at `~/.claude/projects/-home-administrator-Code-tuxlink/memory/` — 9 entries adapted from geographica:
- `user_cameron.md` — user profile.
- `feedback_coaching_mode.md` — surface trigger + mechanics for new workflows.
- `feedback_writing_voice_no_first_person.md` — declarative voice for project docs.
- `feedback_moniker_collision_pre_flight.md` — grep AND git log discipline when picking a moniker.
- `feedback_trust_support_engineer_intuition.md` — invoke bug-hunt-cycle when Cameron names a mechanism class.
- `feedback_browser_smoke_before_ship.md` — `pnpm tauri dev` walk before declaring UI done.
- `feedback_visual_companion_default.md` — launch the visual companion immediately during brainstorming.
- `feedback_handoff_runtime_provenance.md` — probe runtime claims before trusting HANDOFF text.
- `project_branch_model.md` — records the per-task-branch decision.
- `MEMORY.md` — index, auto-loaded by harness.

---

## Engineering controls — what now refuses what

| Layer | What it blocks | Where it lives |
|---|---|---|
| `block-destructive-git.sh` (hook) | `reset --hard`, `push --force`, `--amend`, `--no-verify`, `clean -f`, `branch -D`, `filter-branch`, etc. | `.claude/hooks/` |
| `check-commit-discipline.sh` (hook) | Unsubstituted `<SESSION-MONIKER>` placeholder, missing `Agent:` trailer, direct commits to `main` / `feat/v0.0.1` | `.claude/hooks/` |
| `session-start-briefing.sh` (hook) | Nothing — informational. Prints state at session start. | `.claude/hooks/` |
| GitHub branch protection | Force-push to `main`, deletion of `main` | GitHub repo settings |
| Convention | bd directives that conflict with project commitments | CLAUDE.md `## Tool referee` |

The hooks compose with auto-claude's `safe-git` wrapper (when auto-claude is later adopted) for OS-level defense in depth.

---

## Open Dependabot PRs

The first push to origin triggered Dependabot's first scan. As of handoff time, 5 update jobs were in flight:
- 1 cargo update on `/src-tauri` — likely errors with "no manifest" (Task 1 creates `Cargo.toml`).
- 2 github_actions updates on `/`.
- 2 npm_and_yarn updates on `/`.

Triage: `gh run list --limit 10` and `gh pr list` early in the next session. Dependabot's PRs (if any) follow the per-task-branch model — review and squash-merge into `main` directly (Dependabot is the only legitimate non-task source of commits to main).

---

## What's intentionally deferred

- **bd dolt remote setup** — solo use is fine; revisit when cross-machine bd state mirroring matters.
- **gstack update** — staged in this handoff, runs at session boundary. (Superpowers is already at 5.1.0; no action needed there.)
- **`fewer-permission-prompts` skill run** — premature with no transcript history; defer until after Task 1 lands so it has data to allowlist.
- **Tier-2 governance items** (CONTRIBUTING expansion as we hit edge cases, ADRs as decisions land mid-execution, README rewrite during Task 18) — ride along v0.0.1 execution.
- **Tier-3 governance items** (public roadmap, mdbook docs site, GitHub Discussions, "good first issue" labels) — post-v0.0.1.
- **Auto-claude framework adoption** — possible later; the per-task-branch model and bd are auto-claude-ready when we want it.

---

## Open decisions next session can confirm

1. **Execution model for Task 1**: subagent-driven-development (recommended, plan's REQUIRED SUB-SKILL) or executing-plans (inline)?
2. **bd dolt remote**: set up now or defer?
3. **Branch naming convention**: `task-01-tauri-scaffold` (numeric) vs `bd-wkz/tauri-scaffold` (bd-hash). Both are sanctioned by ADR 0004 — pick one for consistency.
4. **Cost tracking baseline**: pull `cost_report` API for the date range covering this prep-wave session, archive as the v0.0.0 baseline. Optional but recommended for portfolio narrative.

---

## File inventory (this commit)

In `f81b7ad` (31 files, 1532 insertions):

```
.beads/issues.jsonl                                 (auto-exported by bd pre-commit hook)
.claude/hooks/block-destructive-git.sh
.claude/hooks/check-commit-discipline.sh
.claude/hooks/session-start-briefing.sh
.github/.release-please-manifest.json
.github/ISSUE_TEMPLATE/bug_report.yml
.github/ISSUE_TEMPLATE/config.yml
.github/ISSUE_TEMPLATE/feature_request.yml
.github/PULL_REQUEST_TEMPLATE.md
.github/dependabot.yml
.github/release-please-config.json
.github/workflows/release-please.yml
AGENTS.md                                           (added Tool referee summary)
CHANGELOG.md
CLAUDE.md                                           (added Tool referee section)
CODE_OF_CONDUCT.md
CONTRIBUTING.md
LICENSE
SECURITY.md
UPGRADING.md
VERSIONING.md
dev/handoffs/2026-04-22-plan-approved-pending-execution.md  (lichen's, finally tracked)
docs/adr/0001-record-architecture-decisions.md
docs/adr/0002-tauri-react-single-crate.md
docs/adr/0003-no-sqlite-pat-owns-mailbox.md
docs/adr/0004-per-task-branch-model.md
docs/adr/0005-rigorous-semver-via-release-please.md
docs/adr/0006-override-bd-claude-md-defaults.md
docs/adr/README.md
docs/pitfalls/implementation-pitfalls.md            (added Tool Integration / BD-1)
docs/plans/2026-04-22-tuxlink-v0.0.1-plan.md        (Agent:lichen → placeholder, per-task wrap)
```

Memory seed lives outside the repo at `~/.claude/projects/-home-administrator-Code-tuxlink/memory/` (9 entries + MEMORY.md), per Claude Code convention.

---

**If something in this handoff looks wrong tomorrow:** the previous-session agent (alder) wasn't perfect; flag it before acting on it. The override mechanism in particular is new and may have edge cases that surface only on first agent contact. ADR 0006's "Watched failure modes" section lists the signals to watch for.
