[{"description":"Run an ARDOP B2F message exchange with a target for the given routing intent (cms/radio-only/post-office/mesh/p2p; defaults to cms). The ARDOP link is tuned at connect, so this carries no frequency/QSY params. EGRESS (keys the transmitter): requires armed send-authority and an un-tainted session; denied otherwise.","name":"ardop_b2f_exchange","parameters":{"$defs":{"SessionIntentDto":{"description":"Which message POOL / routing a B2F session targets. Mirrors the monolith's\n`SessionIntent` 1:1 (`Cms` / `RadioOnly` / `PostOffice` / `Mesh` / `P2p`);\nthe impl maps it onto `crate::winlink::session::SessionIntent`.\nA B2F exchange always performs a full send+receive round once connected, so\nthis selects the routing pool, not a transfer direction.","oneOf":[{"const":"cms","description":"Global Winlink CMS (Telnet/TLS or transparent relay-to-CMS proxy).","type":"string"},{"const":"radio-only","description":"R pool — RF-only Hybrid network; messages never traverse the internet.","type":"string"},{"const":"post-office","description":"L pool — store-and-forward at a local RMS Relay \"post office\".","type":"string"},{"const":"mesh","description":"MESH — Network Post Office (locally-run RMS Relay / AREDN mesh transport,\nnormal CMS mail pool).","type":"string"},{"const":"p2p","description":"Peer-to-peer — direct station, no CMS, no creds, no routing flag.","type":"string"}]}},"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"intent":{"$ref":"#/$defs/SessionIntentDto","default":"cms","description":"The routing pool for the exchange; defaults to `cms` when omitted."},"target":{"description":"The target station/gateway callsign.","type":"string"}},"required":["target"],"type":"object"}},{"description":"Connect the ARDOP modem to a target station. Optional freq_hz pre-tunes the rig over CAT for the dial (audio-CENTER frequency, Winlink catalog convention - the app converts to the sideband dial; do NOT pre-subtract); optional qsy_candidates supplies an ordered frequency walk (operator-gated). EGRESS (keys the transmitter): requires armed send-authority and an un-tainted session; denied otherwise.","name":"ardop_connect","parameters":{"$defs":{"QsyCandidateDto":{"description":"One QSY (frequency-walk) candidate the agent can supply on a gated\nconnect/exchange: a dial `target` plus the frequency to tune for it. Mirrors\nthe monolith's `DialCandidate` field-for-field (snake_case wire form). An\nomitted/empty candidate list reproduces today's single-dial behavior.","properties":{"freq_hz":{"description":"The frequency in Hz to tune before dialing this candidate; `None` skips\nthe pre-audio CAT tune for it.","format":"uint64","minimum":0,"type":["integer","null"]},"target":{"description":"The dial target (station/gateway callsign) for this candidate.","type":"string"}},"required":["target"],"type":"object"}},"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"freq_hz":{"default":null,"description":"Optional pre-audio CAT tune frequency in Hz for the single dial.","format":"uint64","minimum":0,"type":["integer","null"]},"qsy_candidates":{"default":null,"description":"Optional ordered QSY candidate list; when present + non-empty it\noverrides `target`/`freq_hz` with a frequency walk.","items":{"$ref":"#/$defs/QsyCandidateDto"},"type":["array","null"]},"target":{"description":"The target station/gateway callsign to connect to.","type":"string"}},"required":["target"],"type":"object"}},{"description":"DEPRECATED alias of list_audio_devices - the audio-device list is station-level, not ARDOP-specific (tuxlink-hq9g0). Identical output; prefer list_audio_devices.","name":"ardop_list_audio_devices","parameters":{"properties":{},"type":"object"}},{"description":"Report the Winlink backend (CMS engine) connection status: connected, transport, and state. Read-only. Does NOT report send authority - call server_info to check whether transmitting is currently armed.","name":"backend_status","parameters":{"properties":{},"type":"object"}},{"description":"List the Winlink Request Center query catalog: the on-demand products the operator can request over Winlink. Hundreds of items across categories such as PROPAGATION (HF propagation forecasts), METAR (airport weather), SAT_KEPS (satellite keplerian elements), AURORA, marine/ice forecasts, and ARES/RACES bulletins. Each entry has a category, a filename id (pass these ids to catalog_send_inquiry to stage a request), and a description. This is what to browse to answer 'what can I pull/request from the Request Center'; it is NOT limited to weather/GRIB. App-owned content; does not taint. Read-only.","name":"catalog_list","parameters":{"properties":{},"type":"object"}},{"description":"Stage a Winlink Request Center inquiry for one or more catalog item ids (the filename ids from catalog_list) in the local outbox; returns the staged message id. Use this to request ANY catalog product - propagation forecasts, METAR airport weather, satellite keplerian data, bulletins, marine forecasts, etc., not just GRIB weather. The requested products are delivered to the mailbox after the operator next connects (Arm to send). No transmission occurs here.","name":"catalog_send_inquiry","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"item_ids":{"description":"The catalog item ids to inquire about.","items":{"type":"string"},"type":"array"}},"required":["item_ids"],"type":"object"}},{"description":"Abort/disconnect the CMS connection. Stopping is always allowed (never gated).","name":"cms_abort","parameters":{"properties":{},"type":"object"}},{"description":"Connect to the configured CMS (Winlink common message server). EGRESS: requires armed send-authority and an un-tainted session; denied otherwise.","name":"cms_connect","parameters":{"properties":{},"type":"object"}},{"description":"Read the non-secret ARDOP modem config (host, port, drive level, bandwidth). Read-only.","name":"config_get_ardop","parameters":{"properties":{},"type":"object"}},{"description":"Read the non-secret radio-level rig config (hamlib model, rigctld endpoint, CAT serial, close-serial/live-vfo/qsy flags). Read-only; no secrets.","name":"config_get_rig","parameters":{"properties":{},"type":"object"}},{"description":"Read the non-secret VARA modem config (host, port, bandwidth, drive level). Read-only; no VARA license secrets.","name":"config_get_vara","parameters":{"properties":{},"type":"object"}},{"description":"Read the curated, non-secret top-level config (CMS connect flag, transport, host, callsign, 4-char grid). Read-only; no passwords.","name":"config_read","parameters":{"properties":{},"type":"object"}},{"description":"Set the ARDOP drive level (0..=100). WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise. An out-of-range value is rejected as invalid even when disarmed.","name":"config_set_ardop","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"drive_level":{"description":"Transmit drive level, `0..=100`.","format":"uint8","maximum":255,"minimum":0,"type":"integer"}},"required":["drive_level"],"type":"object"}},{"description":"Set the station grid square (Maidenhead locator). WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise.","name":"config_set_grid","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"grid":{"description":"The Maidenhead grid square to set.","type":"string"}},"required":["grid"],"type":"object"}},{"description":"Set the GPS privacy: broadcast state + position precision. WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise.","name":"config_set_privacy","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"gps_state":{"description":"GPS broadcast state (e.g. `on` / `off`).","type":"string"},"precision":{"description":"Position precision (e.g. a Maidenhead precision selector).","type":"string"}},"required":["gps_state","precision"],"type":"object"}},{"description":"Set the VARA bandwidth in Hz (500/2300/2750). WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise. An out-of-range value is rejected as invalid even when disarmed.","name":"config_set_vara","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"bandwidth_hz":{"description":"VARA bandwidth in Hz; one of `500`, `2300`, `2750`.","format":"uint32","minimum":0,"type":"integer"}},"required":["bandwidth_hz"],"type":"object"}},{"description":"Read one documentation page IN FULL, given the slug from a docs_search hit. This is how you get the actual text - command syntax, connection strings, configuration steps - that docs_search's snippet only hints at. Use docs_search first to find the slug, then docs_read to read the page, then answer FROM the page. If the slug is unknown, the result lists the valid slugs. App-owned content; does not taint. Read-only.","name":"docs_read","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"slug":{"description":"The `slug` of a documentation page, exactly as returned by `docs_search`.","type":"string"}},"required":["slug"],"type":"object"}},{"description":"Search the documentation by keyword. Covers Tuxlink's own user guide, troubleshooting playbooks, and reference material on OTHER Winlink clients (Pat, Winlink Express). Returns ranked hits as {title, slug, snippet}. IMPORTANT: the snippet is only a short fragment around the keyword match and is NOT enough to answer from. To read a page, call docs_read with its slug. App-owned content; does not taint. Read-only.","name":"docs_search","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"query":{"description":"The documentation search query string.","type":"string"}},"required":["query"],"type":"object"}},{"description":"Write a generated report (`content`, markdown or plain text) to the operator's ~/Documents/Tuxlink/reports/ folder under `filename`. You pick the FILENAME, never the directory; `..`/absolute/traversal paths are rejected. Returns the absolute path written. A local file write - NOT a transmission, ungated.","name":"export_report","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"content":{"description":"The report body — markdown or plain text.","type":"string"},"filename":{"description":"Filename for the report (e.g. `sitrep.md`). Relative to the reports\nfolder; `..` / absolute paths are rejected.","type":"string"}},"required":["filename","content"],"type":"object"}},{"description":"List the stations this operator talks to directly (contacts with P2P reachability: exact callsign, tier, RF channels). Telnet endpoint addresses are never included - the agent dials radio channels only. Requires the egress arm - the roster is the operator's private station data, not public directory data. Read-only; does not transmit.","name":"find_peers","parameters":{"properties":{},"type":"object"}},{"description":"Find Winlink RMS gateways by stating your INTENT (the tool selects and bounds the answer; it never dumps the whole catalog). Set `intent` to one of: `recommend` (which gateway should I connect to? - a ranked shortlist with one selected connection each), `explore` (narrow a broad space - returns facet counts + suggested filters, not rows, until the set is small), `lookup` (exact callsign(s)), `aggregate` (server-side counts by band/mode/distance/etc. over the whole matched set), or `export` (write the full set to a user file OUTSIDE this conversation). Constraints go NESTED under the `filters` object (e.g. `{\"intent\": \"explore\", \"filters\": {\"bands\": [\"40m\"], \"modes\": [\"vara-hf\"]}}`) - never as top-level arguments; your grid, the time, and connection history are supplied by the app. A broad `explore` returns a `refinement-required` result: zero rows, the exact total, facet counts, and `suggested_refinements` each carrying a ready-to-send `next_call` - send that `next_call.arguments` object VERBATIM as your next call (its `filters` are already fully merged; do not repeat an unchanged call, it returns the same answer). If you already know what you want, `recommend` with `filters` + a `goal` skips the refinement loop entirely. Omit `snapshot_id` on a first call (never pass the string \"null\"). Public directory data, cached. Read-only; does not transmit.","name":"find_stations","parameters":{"$defs":{"Band":{"description":"An amateur HF band the directory classifies. Variants mirror the `BANDS`\ntable in `mcp_ports.rs` exactly so the engine can map a `Band` back onto the\nexisting string band filter (`any_freq_in_bands`). Kebab/label tokens on the\nwire (`\"40m\"`, `\"160m\"`), so the advertised schema lists exactly the legal set.","enum":["160m","80m","60m","40m","30m","20m","17m","15m","12m","10m"],"type":"string"},"BandwidthClass":{"description":"An occupied-bandwidth class in Hz. Mirrors the classes the existing\n`bandwidth_class` / `channel_passes_bandwidth` filter recognizes (VARA\n500/2300/2750, ARDOP 500/1000/2000). Serialized as the numeric token so the\nwire reads `[\"500\",\"2750\"]`.","enum":["500","1000","2000","2300","2750"],"type":"string"},"BearingSector":{"description":"An 8-point compass bearing sector.","enum":["n","ne","e","se","s","sw","w","nw"],"type":"string"},"ConnectObjective":{"description":"What a `recommend` call optimizes for.","oneOf":[{"const":"estimated-success","description":"Estimated connect success (path reliability + FT-8 + operating-now +\nhistory), ranked by the versioned `connect-now-v1` policy.","type":"string"},{"const":"nearest","description":"Pure great-circle distance, honestly named `nearest-v1` — never presented\nas connection fitness.","type":"string"}]},"DistanceBucket":{"description":"A coarse distance bucket in statute miles (imperial-default audience). The\nengine filters `distance_mi` against [`DistanceBucket::upper_mi`]; `Beyond*`\nhas no upper bound.","enum":["within100mi","within300mi","within600mi","within1200mi","within2500mi","beyond2500mi"],"type":"string"},"Ft8Policy":{"description":"How to treat FT-8 corroboration when ranking/filtering. Replaces the old\n`ft8_evidence: Option<bool>`: `require` keeps only corroborated gateways,\n`prefer` corroborates + ranks up but does not exclude, `ignore` (default)\nserves gateways without evidence.","enum":["ignore","prefer","require"],"type":"string"},"RecommendationGoal":{"description":"The decision `recommend` is answering.","oneOf":[{"description":"\"Which gateway should I connect to right now?\" `at_utc_ms` overrides the\ninjected current time (else the engine uses now).","properties":{"at_utc_ms":{"default":null,"format":"uint64","minimum":0,"type":["integer","null"]},"kind":{"const":"connect-now","type":"string"},"objective":{"$ref":"#/$defs/ConnectObjective"}},"required":["kind","objective"],"type":"object"},{"description":"\"Which gateway should I connect to at this future time?\"","properties":{"at_utc_ms":{"format":"uint64","minimum":0,"type":"integer"},"kind":{"const":"best-at","type":"string"},"objective":{"$ref":"#/$defs/ConnectObjective"}},"required":["kind","at_utc_ms","objective"],"type":"object"}]},"StationExportFormat":{"description":"Export artifact format for the `export` intent.","enum":["json","csv"],"type":"string"},"StationFacet":{"description":"A property a population can be grouped/faceted by (`aggregate.group_by`,\n`explore` facets).","enum":["mode","band","bandwidth-class","distance-bucket","bearing-sector","operating-now"],"type":"string"},"StationFilters":{"description":"Agent-supplied narrowing constraints. All optional; omission means \"no\nconstraint\" on that axis. Breadth is handled by the response contract\n(`refinement-required`), never by silently capping the result.\n\nSerialization omits unconstrained axes (tuxlink-eefln): an echoed filter\nset shows only the constraints in force, so a template never presents\nnull-filled fields a model can pattern-copy into its own arguments.","properties":{"bands":{"description":"Restrict to these HF bands; empty = all.","items":{"$ref":"#/$defs/Band"},"maxItems":16,"type":"array"},"bandwidths":{"description":"Restrict to these occupied-bandwidth classes; empty = all.","items":{"$ref":"#/$defs/BandwidthClass"},"maxItems":5,"type":"array"},"bearing":{"anyOf":[{"$ref":"#/$defs/BearingSector"},{"type":"null"}],"description":"Keep only gateways in this bearing sector from the operator."},"callsign_prefix":{"description":"Keep only gateways whose callsign starts with this prefix (case-folded).","maxLength":12,"type":["string","null"]},"distance":{"anyOf":[{"$ref":"#/$defs/DistanceBucket"},{"type":"null"}],"description":"Keep only gateways at most this far away."},"ft8_policy":{"$ref":"#/$defs/Ft8Policy","description":"FT-8 corroboration policy; default `ignore`."},"history_hours":{"description":"Only gateways heard within this many hours; `None` = no bound.","format":"uint32","minimum":0,"type":["integer","null"]},"modes":{"description":"Restrict to these transports; empty = all.","items":{"$ref":"#/$defs/StationModeDto"},"maxItems":6,"type":"array"},"operating_now":{"description":"When `Some(true)`, keep only gateways operating now (per advertised\noperating hours vs the injected current time); `Some(false)` keep only\noff-hours; `None` no constraint.","type":["boolean","null"]}},"type":"object"},"StationModeDto":{"description":"A Winlink RMS gateway operating mode / transport. Kebab-case on the wire so\nthe agent-facing values read `vara-hf`, `vara-fm`, `ardop-hf`,\n`robust-packet`, etc.\n\n`VaraFm` serializes as `\"vara-fm\"`, the SAME token the monolith\n[`ListingMode::VaraFm`](crate) uses and the frontend `ListingMode` union\ncarries. VARA FM stations reach the agent with the same fidelity the frontend\ngets (they are sourced from the channels JSON API, not a text `/listings/`\npage).","enum":["vara-hf","vara-fm","packet","ardop-hf","pactor","robust-packet"],"type":"string"}},"$schema":"https://json-schema.org/draft/2020-12/schema","oneOf":[{"description":"\"Which gateway should I connect to?\" — a ranked decision answer.","properties":{"candidate_count":{"default":3,"maximum":8,"minimum":1,"type":"integer"},"exclude_candidate_ids":{"default":[],"items":{"maxLength":96,"type":"string"},"maxItems":16,"type":"array"},"filters":{"$ref":"#/$defs/StationFilters","default":{}},"goal":{"$ref":"#/$defs/RecommendationGoal"},"intent":{"const":"recommend","type":"string"}},"required":["intent","goal"],"type":"object"},{"description":"Narrow a broad space by property; returns facets, not rows, until small.","properties":{"filters":{"$ref":"#/$defs/StationFilters","default":{}},"intent":{"const":"explore","type":"string"},"snapshot_id":{"default":null,"maxLength":32,"type":["string","null"]}},"required":["intent"],"type":"object"},{"description":"Known callsign(s) — exact lookup.","properties":{"callsigns":{"items":{"maxLength":16,"type":"string"},"maxItems":16,"type":"array"},"intent":{"const":"lookup","type":"string"},"snapshot_id":{"default":null,"maxLength":32,"type":["string","null"]}},"required":["intent","callsigns"],"type":"object"},{"description":"Server-side counts/statistics over the FULL matched population.","properties":{"filters":{"$ref":"#/$defs/StationFilters","default":{}},"group_by":{"items":{"$ref":"#/$defs/StationFacet"},"maxItems":3,"type":"array"},"intent":{"const":"aggregate","type":"string"}},"required":["intent","group_by"],"type":"object"},{"description":"The full set as a user artifact OUTSIDE the transcript (never model-readable).","properties":{"filters":{"$ref":"#/$defs/StationFilters","default":{}},"format":{"$ref":"#/$defs/StationExportFormat"},"intent":{"const":"export","type":"string"}},"required":["intent","format"],"type":"object"}],"type":"object"}},{"description":"List the amateur stations heard on FT-8 recently, deduplicated: callsign, Maidenhead grid, best signal-to-noise ratio in dB, how many times heard, and when last heard. This is how to answer 'who am I hearing' / 'what stations are on this band'. Requires the listener to be running (see ft8_start_listening). Returns an empty list if nothing has decoded yet. Read-only; does not taint.","name":"ft8_heard_stations","parameters":{"properties":{},"type":"object"}},{"description":"List the audio capture devices the FT-8 listener can use, with the stable id to select. Use when ft8_status reports it is blocked needing a device.","name":"ft8_list_audio_devices","parameters":{"properties":{},"type":"object"}},{"description":"Set the FT-8 band (e.g. \"20m\", \"40m\"). If rig CAT control is configured this QSYs the radio's dial to that band's FT-8 frequency. Does not transmit.","name":"ft8_set_band","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"band":{"description":"The amateur band to set the FT-8 listener to (e.g. `\"20m\"`, `\"40m\"`).","type":"string"}},"required":["band"],"type":"object"}},{"description":"Start the FT-8 listener on the configured band and audio device. RECEIVE-ONLY: this does not transmit and does not require send authority. Returns an error naming what is missing if no audio device is configured.","name":"ft8_start_listening","parameters":{"properties":{},"type":"object"}},{"description":"Report the FT-8 listener's state: whether it is listening, on which band and dial frequency, which audio device, and what is blocking it if it cannot start. Call this before ft8_heard_stations if no stations come back - the listener may simply not be running. Receive-only. Does not taint. Read-only.","name":"ft8_status","parameters":{"properties":{},"type":"object"}},{"description":"Stop the FT-8 listener and release the audio device.","name":"ft8_stop_listening","parameters":{"properties":{},"type":"object"}},{"description":"Report whether the first-run setup wizard has been completed. Read-only.","name":"get_wizard_completed","parameters":{"properties":{},"type":"object"}},{"description":"Stage a GRIB weather-product request in the local outbox; returns the staged message id. No transmission occurs until a later gated connect. Validates the subject header.","name":"grib_send_request","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"lat":{"description":"Request center latitude.","format":"double","type":"number"},"lon":{"description":"Request center longitude.","format":"double","type":"number"},"mode":{"description":"Request mode/product selector.","type":"string"},"subject":{"description":"Subject line for the staged request message.","type":"string"}},"required":["lat","lon","mode","subject"],"type":"object"}},{"description":"Enumerate the STATION'S audio devices. Station/OS-level and mode-agnostic: the same physical device list serves ARDOP, VARA, FT-8, and packet - use this for ANY mode's audio troubleshooting (the number-one VARA failure is transmit-audio routing: wrong device, or a device another app holds). Read-only. Returns `capture`/`playback` ALSA names AND a richer `cards` list - per USB card: name, ALSA name, card index, `vid_pid` (e.g. 0d8c:013a), `bus_path` (distinguishes two identical-name cards on different ports), and `in_use`. To disambiguate, read tuxlink://playbook/audio-setup - the method (VID:PID + bus path + in-use), never a code-side ranking.","name":"list_audio_devices","parameters":{"properties":{},"type":"object"}},{"description":"List message metadata in a mailbox folder. Returns untrusted message senders/subjects, so calling this taints the session (send authority is locked until the operator re-arms).","name":"mailbox_list","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"folder":{"description":"The mailbox folder name to list.","type":"string"}},"required":["folder"],"type":"object"}},{"description":"Move a message between mailbox folders. WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise.","name":"mailbox_move","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"from":{"description":"The source folder.","type":"string"},"id":{"description":"The message id to move.","type":"string"},"to":{"description":"The destination folder.","type":"string"}},"required":["from","to","id"],"type":"object"}},{"description":"Save a message attachment to a destination path (validated to stay inside the attachment base; absolute/parent-traversal/escaping paths are rejected as invalid). WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise.","name":"message_attachment_save","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"dest":{"description":"The destination path (validated to stay inside the attachment base).","type":"string"},"filename":{"description":"The attachment filename within the message.","type":"string"},"folder":{"description":"The folder the message lives in.","type":"string"},"id":{"description":"The message id.","type":"string"}},"required":["folder","id","filename","dest"],"type":"object"}},{"description":"Read a parsed message (headers, body, attachment metadata) by folder + id. Returns untrusted content, so calling this taints the session.","name":"message_read","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"folder":{"description":"The folder the message lives in.","type":"string"},"id":{"description":"The message id to read.","type":"string"}},"required":["folder","id"],"type":"object"}},{"description":"Stage a composed message in the local outbox; returns the staged message id. No transmission occurs until a later gated connect. Validates recipients/subject/body; a malformed value (e.g. a CR/LF in an address) is rejected as invalid.","name":"message_send","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"body":{"description":"Message body.","type":"string"},"cc":{"default":[],"description":"Carbon-copy recipient addresses.","items":{"type":"string"},"type":"array"},"subject":{"description":"Message subject.","type":"string"},"to":{"description":"Primary recipient addresses.","items":{"type":"string"},"type":"array"}},"required":["to","subject","body"],"type":"object"}},{"description":"Disconnect the ARDOP modem. Stopping is always allowed (never gated).","name":"modem_ardop_disconnect","parameters":{"properties":{},"type":"object"}},{"description":"Report modem status with BOTH what is actually RUNNING and what the operator has SELECTED. `running` lists every live session (ARDOP and VARA are independent - both can be live); `kind`/`connected`/`state` describe the primary running modem (`kind`=\"idle\" when nothing runs - it never falls back to `selected`); `conflict` is true when more than one modem runs; `selected` is the operator's persisted target connection (`{session_type, protocol}`) regardless of what is live. Read-only.","name":"modem_get_status","parameters":{"properties":{},"type":"object"}},{"description":"Report whether a P2P peer password is Set (true) or NotSet (false) for the given callsign. Never returns the password itself. Read-only.","name":"p2p_peer_password_status","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"callsign":{"description":"The peer station callsign to check.","type":"string"}},"required":["callsign"],"type":"object"}},{"description":"Read the non-secret packet (AX.25/KISS) config (KISS host/port, baud, TX delay). Read-only.","name":"packet_config_get","parameters":{"properties":{},"type":"object"}},{"description":"Set the packet (AX.25/KISS) connection params (ssid, tcp host/port, tx delay). WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise.","name":"packet_config_set","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"ssid":{"description":"Station SSID.","format":"uint8","maximum":255,"minimum":0,"type":"integer"},"tcp_host":{"description":"KISS TNC TCP host.","type":"string"},"tcp_port":{"description":"KISS TNC TCP port.","format":"uint16","maximum":65535,"minimum":0,"type":"integer"},"txdelay_ms":{"description":"TX delay in milliseconds.","format":"uint32","minimum":0,"type":"integer"}},"required":["ssid","tcp_host","tcp_port","txdelay_ms"],"type":"object"}},{"description":"Connect an AX.25 packet session to a callsign over an optional digipeater path. EGRESS (keys the transmitter): requires armed send-authority and an un-tainted session; denied otherwise.","name":"packet_connect","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"call":{"description":"The destination station callsign.","type":"string"},"path":{"default":[],"description":"Optional ordered digipeater path; empty for a direct connection.","items":{"type":"string"},"type":"array"}},"required":["call"],"type":"object"}},{"description":"Enumerate Bluetooth devices (names + minimized MACs). Read-only.","name":"packet_list_bluetooth_devices","parameters":{"properties":{},"type":"object"}},{"description":"Enumerate serial devices available for a TNC/CAT connection. Read-only.","name":"packet_list_serial_devices","parameters":{"properties":{},"type":"object"}},{"description":"Enable or disable packet listen mode. WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise.","name":"packet_set_listen","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"enabled":{"description":"Whether packet listen mode is enabled.","type":"boolean"}},"required":["enabled"],"type":"object"}},{"description":"Report host platform info (OS, arch, app version) for diagnostics. Read-only.","name":"platform_info","parameters":{"properties":{},"type":"object"}},{"description":"Spotlight a UI element in the main window so the operator can see where it is. Never clicks, navigates, or transmits - display only. Errors list valid anchor IDs when the ID is unknown.","name":"point_at","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"anchor_id":{"description":"Anchor ID from the onboarding registries (e.g. \"ribbon-connect\",\n\"mailbox\", \"contacts\", \"radio-dock\", \"elmer\"). Unknown IDs error with\nthe valid list.","type":"string"}},"required":["anchor_id"],"type":"object"}},{"description":"Set the position source (e.g. gps/manual). WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise.","name":"position_set_source","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"source":{"description":"The position source (e.g. `gps` / `manual`).","type":"string"}},"required":["source"],"type":"object"}},{"description":"Report THE OPERATOR'S current station location - their Maidenhead grid square (precision-reduced to ~4 chars for privacy) and GPS fix status. Call this to answer 'where am I', 'what's near me', or any location-relative question - do NOT ask the operator for their location; Tuxlink already knows it. Read-only.","name":"position_status","parameters":{"properties":{},"type":"object"}},{"description":"Predict HF path reliability/SNR/MUFday by UTC hour from your station to a target grid across the given dials. Offline VOACAP. Read-only.","name":"predict_path","parameters":{"$defs":{"GatewayAntennaDto":{"description":"A gateway's antenna type, used as an optional prediction parameter. Lowercase\non the wire (`beam` / `dipole` / `vertical`).","enum":["beam","dipole","vertical"],"type":"string"}},"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"frequencies_khz":{"description":"Candidate dial frequencies in kHz to predict across.","items":{"format":"double","type":"number"},"type":"array"},"gateway_antenna":{"anyOf":[{"$ref":"#/$defs/GatewayAntennaDto"},{"type":"null"}],"default":null,"description":"The target gateway's antenna type, when known."},"rx_grid":{"description":"The TARGET (receiving) station's Maidenhead grid locator.","type":"string"}},"required":["rx_grid","frequencies_khz"],"type":"object"}},{"description":"Print a report you previously wrote with export_report to a CUPS destination (lp -d <printer>). `filename` is resolved INSIDE the reports folder (only files you exported can be printed); `printer` is a name from printer_list. CUPS auto-filters text/markdown. A local action - NOT a transmission, ungated.","name":"print_document","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"filename":{"description":"Filename of a report previously written by `export_report`.","type":"string"},"printer":{"description":"CUPS destination name (from `printer_list`).","type":"string"}},"required":["printer","filename"],"type":"object"}},{"description":"List CUPS print destinations (lpstat -p -d): each destination's name and whether it is the system default. Read-only. An empty list means no printer / CUPS is available - fall back to export_report.","name":"printer_list","parameters":{"properties":{},"type":"object"}},{"description":"Report the rig's configured state and, best-effort, its live VFO frequency/mode/PTT via a transient rigctld read (never transmits; may report nulls if the rig is unconfigured or its serial is busy with an active session). Read-only.","name":"rig_status","parameters":{"properties":{},"type":"object"}},{"description":"Tune the rig for a Winlink channel over CAT (set VFO + data mode). freq_hz is the channel's audio-CENTER frequency (the value Winlink catalogs publish); the app converts to the sideband dial (center - 1500 Hz on USB) internally - do NOT pre-subtract. EGRESS (commands the radio - same authority class as a transmit): requires armed send-authority and an un-tainted session; denied otherwise.","name":"rig_tune","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"freq_hz":{"description":"The channel's audio-CENTER frequency in Hz (Winlink catalog\nconvention). The app converts to the sideband dial internally\n(tuxlink-9pzaj) — callers must not pre-subtract the 1500 Hz offset.","format":"uint64","minimum":0,"type":"integer"}},"required":["freq_hz"],"type":"object"}},{"description":"The routine AUTHORING catalog: every valid step action (name, description, example params, and whether it transmits / writes config / needs radio or internet), every CONTROL step kind under controls (branch / delay / retry / call / end) with its exact field shape and paste-ready examples - a branch is FLAT ({\"control\": \"branch\", \"on\": \"s1.connected\", \"then\": [step ids], \"else\": [step ids]}, optional op+value pair, never a condition/if wrapper), plus every trigger kind with its fields and a paste-ready example, plus definition_template: one COMPLETE valid routine document (the exact shape routines_save's def accepts; note `routine` is the routine's NAME string and `triggers` is a list). The recommended authoring flow: save the template under your routine's name with routines_save, then build it one fragment at a time - routines_step_add for each step, routines_trigger_set for the schedule - so every piece is validated in isolation and a mistake costs one small call, never the whole document. Call this BEFORE writing a routine - action names are a closed set; invented names (e.g. \"modem.vara.connect\") fail validation with UNKNOWN_ACTION. Optional narrowing: pass action (one action's full record) or section (\"actions\" / \"controls\" / \"trigger_kinds\" / \"definition_template\") to re-read just the contract you are about to use instead of the whole catalog. Read-only.","name":"routines_actions_list","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"action":{"default":null,"description":"Return ONLY this action's full record (exact name, e.g.\n\"radio.connect\"). An unknown name errors and enumerates the valid\nset. Takes precedence over `section`.","type":["string","null"]},"section":{"default":null,"description":"Return ONLY this section of the catalog: \"actions\", \"controls\",\n\"trigger_kinds\", or \"definition_template\".","type":["string","null"]}},"type":"object"}},{"description":"Disable a routine. Never blocked, however invalid the routine currently is - a routine you could not turn off would be a trap.","name":"routines_disable","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"name":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"}},"required":["name"],"type":"object"}},{"description":"Start a DRY run: rehearses a routine with EVERY action swapped for a capability-mirroring fake before anything runs, so nothing real is ever touched - no rig seized, no carrier keyed, no message queued, no gateway dialed. Refused by NOTHING: not a validation error, not a missing automatic-transmit acknowledgment - rehearsing an as-yet-unfit-to-run routine is the whole point. Optional args_json (default \"{}\") and script_json (a JSON object shaping the fake world's per-action outcomes, e.g. {\"outcomes\":{\"radio.connect\":[{\"kind\":\"err\",\"cause\":\"VARA: BUSY\"}]}}; omit for an all-succeeds fake world). Returns the run id (poll routines_run_status / routines_journal_get) plus the validator's findings - informational only, never blocking here.","name":"routines_dry_run","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"args_json":{"default":null,"description":"The run's input args, as a JSON object string; omit for `\"{}\"`.","type":["string","null"]},"name":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"},"script_json":{"default":null,"description":"A JSON object scripting the fake world's per-action outcomes; omit\nfor an all-succeeds fake world.","type":["string","null"]}},"required":["name"],"type":"object"}},{"description":"Enable a routine so its triggers can fire it. Refused (enabled: false, blocked: true, plus the blocking findings - a normal RESULT, not a tool error) when a validation ERROR blocks it; a fleet check runs too, so a schedule collision with an already-enabled routine is reported as a warning without blocking. Enabling an automatic routine that ALREADY carries its design-time transmit acknowledgment (spec §4) succeeds over MCP by design - that acknowledgment covers every invoker, not just the UI.","name":"routines_enable","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"name":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"}},"required":["name"],"type":"object"}},{"description":"Read one routine: {revision, def, edit_protocol}. def is the full definition exactly as stored - the same JSON shape routines_save's def accepts: routine, schema_version, transmit_mode, transmit_ack (if any), triggers, tracks/steps. To MODIFY the routine, follow edit_protocol: use the targeted edit tools it lists (routines_step_update etc.) passing expected_revision = the returned revision; do NOT re-save the whole def with routines_save (a blind resave overwrites concurrent edits). Read-only.","name":"routines_get","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"name":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"}},"required":["name"],"type":"object"}},{"description":"The full, durable step-by-step journal for a run, every entry VERBATIM - a failed step's cause is the actual VARA/CAT/HTTP failure text the action surfaced, never paraphrased. May contain untrusted wire content, so calling this taints the session. Read-only.","name":"routines_journal_get","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"run_id":{"description":"The run id returned by `routines_run` / `routines_dry_run`.","type":"string"}},"required":["run_id"],"type":"object"}},{"description":"List every routine in the automation library: name, transmit mode (attended/automatic), whether it is enabled, and its trigger kinds (schedule/manual). Read-only.","name":"routines_list","parameters":{"properties":{},"type":"object"}},{"description":"Patch a saved routine's envelope fields: transmit_mode (\"attended\"|\"automatic\"), on_interrupted (\"stay\"|\"resume\"), inputs (declared input list). Setting automatic still requires the operator's design-time acknowledgment in the designer before the routine can run - the patch saves and the unacked state is reported, exactly like a whole-document save; leaving automatic revokes any standing acknowledgment. Renaming is routines_rename, not a field here. The routine must be DISABLED.","name":"routines_meta_set","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"patch":{"description":"Envelope fields to patch, as a JSON OBJECT: `transmit_mode`\n(\"attended\"|\"automatic\"), `on_interrupted` (\"stay\"|\"resume\"),\n`inputs`. Renaming is `routines_rename`, not a metadata patch.","type":"object"},"routine":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"}},"required":["routine","patch"],"type":"object"}},{"description":"Rename a routine transactionally: the definition, its enabled state, and call steps in OTHER routines that referenced the old name all migrate in one operation (rewritten callers are reported in callers_updated). Works on an enabled routine - the content is unchanged, so its validation state is identical. Refused when the new name is taken or not kebab-case.","name":"routines_rename","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"new_name":{"description":"The new name (kebab-case, like every routine name).","type":"string"},"routine":{"description":"The routine's CURRENT name.","type":"string"}},"required":["routine","new_name"],"type":"object"}},{"description":"Start a REAL run of a routine by name, with optional JSON-object args_json (default \"{}\"). Refused when a validation error blocks it, or when the routine is automatic-transmit and lacks its design-time acknowledgment (spec §4) - that acknowledgment is recorded ONLY by a UI act in the routine's Settings, no parameter here can supply it, so this specific refusal is expected and persists until the OPERATOR acknowledges it; the refusal message is surfaced to you verbatim, do not paraphrase it back. An automatic routine that ALREADY carries the acknowledgment runs over MCP exactly as it would from the UI - this is by design, not a gap: the acknowledgment covers every invoker. Returns the run id; poll routines_run_status / routines_journal_get.","name":"routines_run","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"args_json":{"default":null,"description":"The run's input args, as a JSON object string; omit for `\"{}\"`.","type":["string","null"]},"name":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"}},"required":["name"],"type":"object"}},{"description":"Fast in-memory status of a run: routine name, whether it is a dry run, and its state (pending/running/waiting/awaiting_consent/awaiting_radio/completed/failed/cancelled/interrupted). Returns null for an unknown run id. For the full step-by-step record use routines_journal_get. Read-only.","name":"routines_run_status","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"run_id":{"description":"The run id returned by `routines_run` / `routines_dry_run`.","type":"string"}},"required":["run_id"],"type":"object"}},{"description":"Save a WHOLE routine definition from def (a JSON OBJECT - the same shape routines_get returns in its def field and routines_actions_list's definition_template shows: routine [the NAME string], schema_version, transmit_mode, triggers [a list], tracks[].steps). Give exactly one of def (preferred, a JSON OBJECT - a stringified object is tolerated and parsed) or def_json (deprecated string form); never both. Best for bootstrap: save the definition_template under your routine's name, then build it step-by-step with routines_step_add / routines_step_update / routines_trigger_set - each fragment edit is validated in isolation and a mistake costs one small call, not the whole document. NEVER refused by validation findings - a half-written draft still saves, and the result's findings/blocked say what is wrong so you can iterate. Refused when the definition fails to parse, its routine name is invalid, or expected_revision is stale (REVISION_CONFLICT).","name":"routines_save","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"def":{"default":null,"description":"The routine definition as a JSON OBJECT (the same shape `routines_get`\nreturns in its `def` field). Preferred — give exactly one of `def` or\n`def_json`, never both, and give the object itself, not a string\ncontaining it.","type":"object"},"def_json":{"default":null,"description":"DEPRECATED string form of `def` (JSON-in-a-string). Accepted for one\nrelease for old callers; use `def` instead.","type":["string","null"]},"expected_revision":{"default":null,"description":"Optional lost-update check: the `revision` a prior `routines_get` /\nsave returned. A stale value refuses the save (REVISION_CONFLICT)\ninstead of silently overwriting someone else's change.","type":["string","null"]}},"type":"object"}},{"description":"Insert ONE step into a saved routine (the fragment-edit path - no whole-document rewrite). step is a JSON OBJECT ({\"action\": ..., \"params\": {...}} or {\"control\": ...}; a branch is FLAT: {\"control\": \"branch\", \"on\": \"s1.connected\", \"then\": [step ids], \"else\": [step ids]} with on a bare path (no $) and an optional op (eq|ne|lt|lte|gt|gte) + value pair); omit its id to have one assigned and returned as step_id. Place with exactly one of: track (append - lands BEFORE the track's trailing end step when one exists, so appended steps always run), after_step_id (splice after), or branch_step_id+branch_arm (into a branch's then/else arm - arm membership and position land atomically). The routine must be DISABLED (ROUTINE_ENABLED otherwise - disable, edit, re-enable). The result is SAVED even with error findings (errors block enable/run, never save); step_findings carries the validator's verdict on YOUR step - fix those before reporting done.","name":"routines_step_add","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"after_step_id":{"default":null,"description":"Splice immediately after this step id. One of the three placements.","type":["string","null"]},"branch_after_step_id":{"default":null,"description":"Position within the arm: after this arm member (or the branch's own id\nfor the front). Omit to append to the arm.","type":["string","null"]},"branch_arm":{"default":null,"description":"`\"then\"` or `\"else\"` — required with `branch_step_id`.","type":["string","null"]},"branch_step_id":{"default":null,"description":"Insert into this branch step's arm (with `branch_arm`). One of the\nthree placements.","type":["string","null"]},"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"routine":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"},"step":{"description":"The step as a JSON OBJECT: `{\"id\": \"s3\", \"action\": \"local.log\",\n\"params\": {...}}` or a control step. A branch is FLAT:\n`{\"id\": \"s4\", \"control\": \"branch\", \"on\": \"s1.connected\", \"then\":\n[\"s5\"], \"else\": []}` — `on` is a bare variable path (no `$` prefix,\nno condition/if/when wrapper object), a comparison adds `op`\n(eq|ne|lt|lte|gt|gte) and `value` together, and `then`/`else` are\nLISTS of step ids. Omit `id` to have one assigned and returned.","type":"object"},"track":{"default":null,"description":"Append to this track (by name). One of the three placements.","type":["string","null"]}},"required":["routine","step"],"type":"object"}},{"description":"Reposition ONE existing step in a saved routine - one atomic operation, no remove/re-add dance through broken-reference states. Same placement fields as routines_step_add: exactly one of track (append to that track), after_step_id, or branch_step_id+branch_arm (old arm membership is scrubbed, new membership set). The routine must be DISABLED.","name":"routines_step_move","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"after_step_id":{"default":null,"description":"Splice immediately after this step id. One of the three placements.","type":["string","null"]},"branch_after_step_id":{"default":null,"description":"Position within the arm: after this arm member (or the branch's own id\nfor the front). Omit to append to the arm.","type":["string","null"]},"branch_arm":{"default":null,"description":"`\"then\"` or `\"else\"` — required with `branch_step_id`.","type":["string","null"]},"branch_step_id":{"default":null,"description":"Insert into this branch step's arm (with `branch_arm`). One of the\nthree placements.","type":["string","null"]},"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"routine":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"},"step_id":{"description":"The step to reposition.","type":"string"},"track":{"default":null,"description":"Append to this track (by name). One of the three placements.","type":["string","null"]}},"required":["routine","step_id"],"type":"object"}},{"description":"Remove ONE step from a saved routine. Branch arms referencing it are scrubbed automatically (reported in scrubbed) so a later step add can never silently inherit stale arm membership; a retry step wrapping it is NOT repaired - the validator's finding tells you to fix or remove the retry. The routine must be DISABLED.","name":"routines_step_remove","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"routine":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"},"step_id":{"description":"The step to remove.","type":"string"}},"required":["routine","step_id"],"type":"object"}},{"description":"Patch ONE step of a saved routine: patch is a JSON OBJECT shallow-merged onto the step - params replaces wholesale, scalar fields (action, timeout_s, on_radio_busy, a branch's then/else lists, ...) patch individually, null clears an optional field. The step's id and its action-vs-control kind cannot change (remove and re-add instead). The routine must be DISABLED. The result is SAVED even with error findings; step_findings carries the validator's verdict on the touched step - fix those before reporting done.","name":"routines_step_update","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"patch":{"description":"Fields to shallow-merge onto the step, as a JSON OBJECT. `params`\nreplaces wholesale; scalar fields (`action`, `timeout_s`,\n`on_radio_busy`, branch `then`/`else`, ...) patch individually; a\n`null` clears an optional field. A branch condition is FLAT fields on\nthe step: `on` (a bare variable path, no `$`) with `op`\n(eq|ne|lt|lte|gt|gte) and `value` together or absent — there is no\ncondition/if/when wrapper object. The `id` and the action-vs-control\nkind cannot change — remove and re-add instead.","type":"object"},"routine":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"},"step_id":{"description":"The step to patch.","type":"string"}},"required":["routine","step_id","patch"],"type":"object"}},{"description":"Add a new empty parallel track to a saved routine (tracks run concurrently). The routine must be DISABLED.","name":"routines_track_add","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"routine":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"},"track":{"description":"The track name.","type":"string"}},"required":["routine","track"],"type":"object"}},{"description":"Remove a track AND every step it held from a saved routine; branch arms elsewhere referencing those steps are scrubbed (reported in scrubbed). The last track cannot be removed. The routine must be DISABLED.","name":"routines_track_remove","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"routine":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"},"track":{"description":"The track name.","type":"string"}},"required":["routine","track"],"type":"object"}},{"description":"Replace a saved routine's trigger list wholesale: triggers is the FULL new list, e.g. [{\"type\": \"schedule\", \"every\": \"30m\", \"align\": \"hour\", \"window\": \"06:00-22:00\", \"if_missed\": \"skip\"}] or [{\"type\": \"manual\"}]. routines_actions_list documents every trigger kind and field. The routine must be DISABLED - which also means a trigger change can never fire the old steps under a new schedule; the schedule anchors when you re-enable.","name":"routines_trigger_set","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"expected_revision":{"default":null,"description":"Optional lost-update check (see `routines_save`).","type":["string","null"]},"routine":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"},"triggers":{"description":"The FULL replacement trigger list: `[{\"type\": \"manual\"}]` or\n`[{\"type\": \"schedule\", \"every\": \"30m\", \"align\": \"hour\", ...}]`.","type":"array"}},"required":["routine","triggers"],"type":"object"}},{"description":"Validate one routine by name against the live station - the SAME validator routines_save/routines_run use (one validator, no privileged path). Returns every finding: code, severity (error/warning), and a message that names the offending entity verbatim. Does not save or run anything. Read-only.","name":"routines_validate","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"name":{"description":"The routine's name, exactly as `routines_list` reports it.","type":"string"}},"required":["name"],"type":"object"}},{"description":"Stage a form submission in the local outbox; returns the staged message id. No transmission occurs until a later gated connect. Validates recipients and headers.","name":"send_form","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"cc":{"default":[],"description":"Carbon-copy recipient addresses.","items":{"type":"string"},"type":"array"},"field_values":{"additionalProperties":{"type":"string"},"default":{},"description":"The form's field name → value map.","type":"object"},"form_id":{"description":"The catalog form id.","type":"string"},"grid_square":{"description":"The sender's grid square.","type":"string"},"senders_callsign":{"description":"The sender's callsign.","type":"string"},"to":{"description":"Primary recipient addresses.","items":{"type":"string"},"type":"array"}},"required":["form_id","to","senders_callsign","grid_square"],"type":"object"}},{"description":"Report the operator's SEND-AUTHORITY state - whether send authority is currently ARMED (armed=true means you MAY transmit; false means you may only stage), how many seconds of the armed window remain, whether the session is TAINTED (which locks sending), and if tainted, taint_reason (a content-free token like \"message_read\" naming why) - plus the Tuxlink app name/version. IMPORTANT: taint DOMINATES arming - if tainted=true you cannot transmit even if armed=true, and the ONLY unlock is the operator re-arming, which DISCARDS the current conversation (it is a quarantine, not a resume); waiting for the timer does nothing. If not tainted but not armed, ask the operator to ARM (that preserves the conversation and you can continue). Call this to check whether you are authorized to transmit right now, e.g. before deciding whether to connect+send or to stage-and-report. Read-only; does not transmit or change any state.","name":"server_info","parameters":{"properties":{},"type":"object"}},{"description":"Snapshot the current session log. May contain untrusted wire content, so calling this taints the session.","name":"session_log_snapshot","parameters":{"properties":{},"type":"object"}},{"description":"Report the STORED space-weather indices (SFI/A/K) and the sunspot number used for predictions. IMPORTANT: this reads a cached snapshot - it does not fetch anything, and the data may be old. Always check the returned `source` and `updated_at_ms` before presenting these as current. `source` is: \"swpc\" (from the internet), \"rf-wwv\" or \"rf-wwv-voice\" (decoded from the radio), or \"shipped\" (a fallback that shipped with the app and has NEVER been updated - do not present shipped defaults as current conditions). If the data is stale or from shipped defaults, tell the operator and offer wwv_capture_offair, which refreshes it over their own radio with no internet. Read-only; does not taint.","name":"solar_conditions","parameters":{"properties":{},"type":"object"}},{"description":"Search mailbox messages by query, optionally scoped to a folder and limited. Returns untrusted message content, so calling this taints the session.","name":"tauri_search_run","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"folder":{"default":null,"description":"Optional folder to scope the search to.","type":["string","null"]},"limit":{"default":null,"description":"Optional maximum number of results to return.","format":"uint32","minimum":0,"type":["integer","null"]},"query":{"description":"The search query string.","type":"string"}},"required":["query"],"type":"object"}},{"description":"Enumerate mailbox folders and their message counts. Structural metadata; does not taint. Read-only.","name":"user_folders_list","parameters":{"properties":{},"type":"object"}},{"description":"Run a VARA B2F message exchange with a target for the given routing intent (cms/radio-only/post-office/mesh/p2p; defaults to cms). Optional freq_hz (audio-CENTER frequency, Winlink catalog convention - converted to the sideband dial internally on VARA HF; do NOT pre-subtract) / qsy_candidates pre-tune + QSY-walk the dial (VARA connects + tunes + exchanges in one call). Optional engine (vara-hf default, vara-fm for FM peers) selects which VARA engine dials - take it from the target peer channel's transport field. EGRESS (keys the transmitter): requires armed send-authority and an un-tainted session; denied otherwise.","name":"vara_b2f_exchange","parameters":{"$defs":{"QsyCandidateDto":{"description":"One QSY (frequency-walk) candidate the agent can supply on a gated\nconnect/exchange: a dial `target` plus the frequency to tune for it. Mirrors\nthe monolith's `DialCandidate` field-for-field (snake_case wire form). An\nomitted/empty candidate list reproduces today's single-dial behavior.","properties":{"freq_hz":{"description":"The frequency in Hz to tune before dialing this candidate; `None` skips\nthe pre-audio CAT tune for it.","format":"uint64","minimum":0,"type":["integer","null"]},"target":{"description":"The dial target (station/gateway callsign) for this candidate.","type":"string"}},"required":["target"],"type":"object"},"SessionIntentDto":{"description":"Which message POOL / routing a B2F session targets. Mirrors the monolith's\n`SessionIntent` 1:1 (`Cms` / `RadioOnly` / `PostOffice` / `Mesh` / `P2p`);\nthe impl maps it onto `crate::winlink::session::SessionIntent`.\nA B2F exchange always performs a full send+receive round once connected, so\nthis selects the routing pool, not a transfer direction.","oneOf":[{"const":"cms","description":"Global Winlink CMS (Telnet/TLS or transparent relay-to-CMS proxy).","type":"string"},{"const":"radio-only","description":"R pool — RF-only Hybrid network; messages never traverse the internet.","type":"string"},{"const":"post-office","description":"L pool — store-and-forward at a local RMS Relay \"post office\".","type":"string"},{"const":"mesh","description":"MESH — Network Post Office (locally-run RMS Relay / AREDN mesh transport,\nnormal CMS mail pool).","type":"string"},{"const":"p2p","description":"Peer-to-peer — direct station, no CMS, no creds, no routing flag.","type":"string"}]},"VaraEngineDto":{"description":"Which VARA engine an agent egress dial should use. Mirrors the monolith's\n`TransportKind::VaraHf` / `TransportKind::VaraFm` split (Task 4); `None` at\nthe call site maps to [`VaraEngineDto::VaraHf`] (backward-compatible with\nevery existing caller). Take this from the target peer channel's\n`transport` field — do not guess.","enum":["vara-hf","vara-fm"],"type":"string"}},"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"engine":{"anyOf":[{"$ref":"#/$defs/VaraEngineDto"},{"type":"null"}],"default":null,"description":"Which VARA engine the target channel uses (`vara-hf` default, `vara-fm`\nfor FM peers) — take it from the peer channel's `transport` field."},"freq_hz":{"default":null,"description":"Optional pre-audio CAT tune frequency in Hz for the single dial.","format":"uint64","minimum":0,"type":["integer","null"]},"intent":{"$ref":"#/$defs/SessionIntentDto","default":"cms","description":"The routing pool for the exchange; defaults to `cms` when omitted."},"qsy_candidates":{"default":null,"description":"Optional ordered QSY candidate list; when present + non-empty it\noverrides `target`/`freq_hz` with a frequency walk.","items":{"$ref":"#/$defs/QsyCandidateDto"},"type":["array","null"]},"target":{"description":"The target station/gateway callsign.","type":"string"}},"required":["target"],"type":"object"}},{"description":"Report whether this Tuxlink build ships the VARA-under-WINE setup engine (true means vara_install_start can run here). x86_64 Linux only. Read-only; does not transmit.","name":"vara_engine_available","parameters":{"properties":{},"type":"object"}},{"description":"Apply [section] key = value assignments to VARA's own VARA.ini via the stop-edit-start lifecycle (tuxlink-iww9r): stops any VARA this app manages (never one it cannot attribute), backs the file up (timestamped), writes atomically, then relaunches VARA and verifies its TCP command port. WRITE: requires armed send-authority (Tier-2 remediation) and an un-tainted session; denied otherwise. NOT a transmission, but it BOUNCES the modem: refused while a VARA session is open, refused if an unmanaged VARA holds the port, and refused (pre-mutation) for a vara2 instance whose command port is unknown. Args: prefix / instance (as in vara_ini_read), edits (list of {section, key, value}; values are single-line), relaunch (default true; false = edit only). Returns {ini_path, backup_path, created, applied, relaunched, cmd_port}. This is how you set the Soundcard Input/Output Device Name, ALC Drive Level, PTT/CAT config, or ports - read vara_ini_read FIRST to see current values.","name":"vara_ini_apply","parameters":{"$defs":{"VaraIniEditDto":{"description":"One `[section] key = value` assignment for `vara_ini_apply`.","properties":{"key":{"description":"Key exactly as VARA writes it, e.g. `Output Device Name`.","type":"string"},"section":{"description":"INI section name without brackets, e.g. `Soundcard`.","type":"string"},"value":{"description":"New value, verbatim. Single-line only; control characters are rejected.","type":"string"}},"required":["section","key","value"],"type":"object"}},"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"edits":{"description":"The assignments to write. May be empty only when `relaunch` is true\n(a pure bounce).","items":{"$ref":"#/$defs/VaraIniEditDto"},"type":"array"},"instance":{"description":"`\"primary\"` (drive_c/\"VARA HF\" or drive_c/VARA) or `\"vara2\"`\n(drive_c/VARA2); absent → primary.","type":["string","null"]},"prefix":{"description":"WINE prefix path; absent → the engine default\n(`~/.local/share/wine-vara/prefix`). A leading `~/` is expanded.","type":["string","null"]},"relaunch":{"description":"Relaunch VARA after the edit and verify its cmd port. Default true.","type":["boolean","null"]}},"required":["edits"],"type":"object"}},{"description":"Read VARA's own VARA.ini (the file VARA itself persists ALL of its config to: [Soundcard] Input/Output Device Name + ALC Drive Level, [PTT] Rig/PTTPort/CATPort/Baud, [Setup] TCP Command Port, [Position], ...). Returns the raw INI text REDACTED: the paid Registration Code and Password encryption values are masked and cannot be read through this tool. Args: prefix (WINE prefix path; absent = the engine default ~/.local/share/wine-vara/prefix), instance (\"primary\" = drive_c/'VARA HF' or drive_c/VARA, \"vara2\" = drive_c/VARA2; absent = primary). Read-only local config - does not launch VARA, does not transmit, does not taint. Use vara_ini_apply to change values.","name":"vara_ini_read","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"instance":{"description":"`\"primary\"` or `\"vara2\"`; absent → primary.","type":["string","null"]},"prefix":{"description":"WINE prefix path; absent → the engine default\n(`~/.local/share/wine-vara/prefix`). A leading `~/` is expanded.","type":["string","null"]}},"type":"object"}},{"description":"Install VARA HF under WINE from a user-supplied installer .exe path (x86_64 Linux only). This runs a PRIVILEGED install: pkexec prompts the OPERATOR for their OS password at the machine - you cannot supply it. NON-TRANSMIT: it provisions software (apt/winetricks/wine) and never keys a radio, so it does NOT require armed send authority. The operator must FIRST download the proprietary VARA .exe themselves (rosmodem / winlink.org) - Tuxlink cannot include it. Read tuxlink://playbook/vara-wine-setup FIRST, then call vara_engine_available + vara_install_status to check state before invoking this. Runs for several minutes and returns the final install summary.","name":"vara_install_start","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"installer_path":{"description":"Filesystem path to the user-downloaded VARA HF installer `.exe`.","type":"string"}},"required":["installer_path"],"type":"object"}},{"description":"Probe whether VARA HF is already provisioned under WINE on this host: returns `ready` plus each install-pipeline checkpoint's state (deps -> prefix -> vara -> vb6 -> ocx -> verify -> autostart). Offline and read-only - never launches VARA, never touches the network, never transmits. Read tuxlink://playbook/vara-wine-setup for the full workflow.","name":"vara_install_status","parameters":{"properties":{},"type":"object"}},{"description":"Open the VARA session: connect the TCP transport to the local VARA engine and register the station callsign (MYCALL). Optional engine (vara-hf default, vara-fm for FM peers) selects which VARA engine to open - take it from the target peer channel's transport field. Pre-air by itself (no RF), but it stands up a transmit-capable surface: requires armed send-authority and an un-tainted session; denied otherwise. Required before vara_b2f_exchange; close with vara_stop_session.","name":"vara_open_session","parameters":{"$defs":{"SessionIntentDto":{"description":"Which message POOL / routing a B2F session targets. Mirrors the monolith's\n`SessionIntent` 1:1 (`Cms` / `RadioOnly` / `PostOffice` / `Mesh` / `P2p`);\nthe impl maps it onto `crate::winlink::session::SessionIntent`.\nA B2F exchange always performs a full send+receive round once connected, so\nthis selects the routing pool, not a transfer direction.","oneOf":[{"const":"cms","description":"Global Winlink CMS (Telnet/TLS or transparent relay-to-CMS proxy).","type":"string"},{"const":"radio-only","description":"R pool — RF-only Hybrid network; messages never traverse the internet.","type":"string"},{"const":"post-office","description":"L pool — store-and-forward at a local RMS Relay \"post office\".","type":"string"},{"const":"mesh","description":"MESH — Network Post Office (locally-run RMS Relay / AREDN mesh transport,\nnormal CMS mail pool).","type":"string"},{"const":"p2p","description":"Peer-to-peer — direct station, no CMS, no creds, no routing flag.","type":"string"}]},"VaraEngineDto":{"description":"Which VARA engine an agent egress dial should use. Mirrors the monolith's\n`TransportKind::VaraHf` / `TransportKind::VaraFm` split (Task 4); `None` at\nthe call site maps to [`VaraEngineDto::VaraHf`] (backward-compatible with\nevery existing caller). Take this from the target peer channel's\n`transport` field — do not guess.","enum":["vara-hf","vara-fm"],"type":"string"}},"$schema":"https://json-schema.org/draft/2020-12/schema","properties":{"engine":{"anyOf":[{"$ref":"#/$defs/VaraEngineDto"},{"type":"null"}],"default":null,"description":"Which VARA engine the target channel uses (`vara-hf` default, `vara-fm`\nfor FM peers) — take it from the peer channel's `transport` field."},"intent":{"$ref":"#/$defs/SessionIntentDto","default":"cms","description":"The routing pool for the session; defaults to `cms` when omitted."}},"type":"object"}},{"description":"Deep READ-ONLY probe of the VARA modem: connect the command port and read its startup banner / VERSION reply. Returns `classification`: \"down\" (no TCP), \"socket-not-vara\" (something is listening but is not VARA), or \"vara-ok\" (a real VARA answered), plus the raw `banner`. NEVER sends MYCALL/BW/LISTEN, never opens the data port, never transmits. Use after `vara_status.reachable` to confirm a real VARA before a send attempt.","name":"vara_probe","parameters":{"properties":{},"type":"object"}},{"description":"Report VARA modem status: connected, bandwidth, and state. Read-only. Also reports `reachable` (true/false/null): command-port (8300) reachability - true = the cmd port answered (or a session is Open), false = no answer, null = unknown (session busy, probe skipped rather than made to wait). cmd-reachable is NOT ready-to-send; use `vara_probe` to confirm a real VARA is answering.","name":"vara_status","parameters":{"properties":{},"type":"object"}},{"description":"Stop the active VARA session. Stopping is always allowed (never gated).","name":"vara_stop_session","parameters":{"properties":{},"type":"object"}},{"description":"Verify the live CMS connection with a network round-trip. EGRESS: requires armed send-authority and an un-tainted session; denied otherwise.","name":"verify_cms_connection","parameters":{"properties":{},"type":"object"}},{"description":"Capture the current space-weather bulletin from the WWV time station over the operator's OWN RADIO and update the stored indices. This needs NO internet - it is how to refresh space weather when the operator is off-grid. Receive-only: it tunes the radio to WWV and listens, it does not transmit. Takes about a minute (it waits for the next bulletin). Requires rig CAT control (see wwv_offair_available). If it returns no_copy=true, audio was captured but the decode was not confident; the indices were not changed. Does not taint.","name":"wwv_capture_offair","parameters":{"properties":{},"type":"object"}},{"description":"Report whether off-air WWV capture is possible: it requires rig CAT control so Tuxlink can tune the radio to WWV. Call before wwv_capture_offair. Read-only; does not taint.","name":"wwv_offair_available","parameters":{"properties":{},"type":"object"}}]