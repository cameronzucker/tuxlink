{
  "routine": "unsatisfiable-var-demo",
  "schema_version": 1,
  "transmit_mode": "attended",
  "on_interrupted": "stay",
  "inputs": [],
  "triggers": [{ "type": "manual" }],
  "tracks": [
    {
      "name": "t1",
      "steps": [
        { "id": "s1", "action": "local.note", "params": { "x": "$s2.out" } },
        { "id": "s2", "action": "local.note", "params": {} },
        { "id": "e1", "control": "end", "failed": false }
      ]
    }
  ]
}
