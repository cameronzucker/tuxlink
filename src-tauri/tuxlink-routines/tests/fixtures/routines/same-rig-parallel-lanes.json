{
  "routine": "same-rig-parallel-lanes-demo",
  "schema_version": 1,
  "transmit_mode": "attended",
  "on_interrupted": "stay",
  "inputs": [],
  "triggers": [{ "type": "manual" }],
  "tracks": [
    {
      "name": "connect-cycle",
      "steps": [
        { "id": "s1", "action": "radio.connect", "params": {} },
        { "id": "e1", "control": "end", "failed": false }
      ]
    },
    {
      "name": "listen-cycle",
      "steps": [
        { "id": "s2", "action": "radio.connect", "params": {} },
        { "id": "e2", "control": "end", "failed": false }
      ]
    }
  ]
}
