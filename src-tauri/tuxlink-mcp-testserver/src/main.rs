//! tuxlink-mcp-testserver tier-2 harness (stub).
//!
//! The real implementation (MCP phase 3.1 Task 6) builds an `McpState` with a
//! mock backend and serves the MCP router over UDS without Tauri. This stub
//! keeps the workspace member compiling until then.
fn main() {}
