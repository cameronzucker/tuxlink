{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "wizard",
  "description": "First-run wizard cluster commands (Tasks 9 + 10 + 11 + 11.5) — scoped to main window only per spec §3.7. External links (Register at winlink.org) are constrained to the allowlist.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-open",
      "allow": [
        { "url": "https://winlink.org/**" },
        { "url": "https://www.winlink.org/**" },
        { "url": "https://github.com/cameronzucker/tuxlink/**" }
      ]
    }
  ]
}
