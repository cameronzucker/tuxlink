{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "logging",
  "description": "Logging window (label 'logging', spec §8.1). Grants event subscription for probe-snapshot push, window-manipulation for custom titlebar, shell:open for log-directory reveal, dialog:save for export archive.",
  "windows": ["logging"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-is-maximized",
    "shell:allow-open",
    "dialog:allow-save",
    "dialog:allow-open"
  ]
}
