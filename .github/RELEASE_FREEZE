FROZEN 2026-07-08 — accumulating the agent-operability cluster into one 0.86.0 release.

The cluster (surfaced by the live VARA HF agent-mode tests) lands as several
related PRs that should ship together, not as separate nightly pre-releases:

  - tuxlink-pf6re  — graceful egress denial + arm/taint perception (PR #1053)
  - tuxlink-7ppfq  — VARA reachability + vara_probe + active-modem SoT (perception-only)
  - tuxlink-z2nwx  — agent print (CUPS) + report export
  - tuxlink-77seh  — agent audio-device inspection + VARA setup guidance

release-please keeps accumulating these into the pending 0.86.0 release PR while
this file exists; the nightly release-merge cron + promotion are paused.

TO RESUME: delete this file (operator decision, once the cluster has landed).
Then the next nightly release-merge cuts 0.86.0 with the whole cluster at once.
