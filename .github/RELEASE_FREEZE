Releases frozen during the multiple/tactical-callsigns epic (tuxlink-zjne).

Why: transmit requires an authenticated active identity (Phase 3, tuxlink-0063,
shipped) but the code that authenticates one on launch is Phase 6
(tuxlink-5ekg), not yet landed — so a release cut now cannot transmit
(tuxlink-yyii). v0.55.0 auto-tagged that broken state.

While this file exists, the release-please workflow skips entirely (no release
PR, no tag). Delete this file in a PR to resume automated releases once the
epic lands and transmit works end-to-end.
